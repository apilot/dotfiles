# Шаблон ~/.env для aider (скопируйте на НОВОЙ машине: cp ~/.env.example ~/.env)
# ~/.env нужен ТОЛЬКО если окружение не даёт ключ само (см. обёртку:
# она берёт OPENAI_API_KEY из Z_AI_API_KEY автоматически).
#
# OPENAI_API_KEY=<ваш ключ z.ai>
# OPENAI_BASE_URL=https://api.z.ai/api/coding/paas/v4
